
%posibles conversiones

%lee el input q ingresa el usuario -> lista de atomos
userInput2List(L):-read_line_to_codes(user_input, Cs), atom_codes(A,Cs), atomic_list_concat(L, " ", A).

%lee el input que ingresa el usuario -> string de atomos
userInput2String(A):-read_line_to_codes(user_input, Cs), atom_codes(A,Cs).

list2String(List, String):-atomic_list_concat(List, " ", String).

string2AtomList(X, L):- atom_codes(X, A), atomic_list_concat(L, " ", X).
